services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: houses-web
    networks:
      - detki-prod-network
    restart: unless-stopped

networks:
  detki-prod-network:
    external: true
